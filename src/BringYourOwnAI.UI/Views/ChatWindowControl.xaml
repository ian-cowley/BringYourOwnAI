<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:vm="clr-namespace:BringYourOwnAI.UI.ViewModels"
              xmlns:local="clr-namespace:BringYourOwnAI.UI.Views"
              mc:Ignorable="d" 
              DataType="MainViewModel">
    <Grid Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" TextElement.Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}">
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </Grid.Resources>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" x:Name="SidebarColumn" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Sidebar -->
        <Border Grid.Column="0" Grid.RowSpan="2" Width="200" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="0,0,1,0" 
                Visibility="{Binding Chat.IsSidebarVisible, FallbackValue=Visible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid  DataContext="{Binding Conversations}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Button Content="+ New Chat" Command="{Binding NewConversationCommand}" Margin="5" Padding="5" />

                <ListBox Grid.Row="1" ItemsSource="{Binding Conversations}" SelectedItem="{Binding SelectedConversation}"
                         Background="Transparent" BorderThickness="0" >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Title}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                <Button Grid.Column="1" Content="✕" Command="{Binding DataContext.DeleteConversationCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}" Background="Transparent" Foreground="Gray" BorderThickness="0" Margin="5,0" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1" Grid.Row="0">
            <!-- Chat View -->
            <Grid Visibility="{Binding IsChatActive, Converter={StaticResource BooleanToVisibilityConverter}}" DataContext="{Binding Chat}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <Border Grid.Row="0" Padding="10" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
                    <DockPanel>
                        <Button Content="☰" Command="{Binding ToggleSidebarCommand}" Background="Transparent" BorderThickness="0"  Margin="0,0,10,0" />
                        <TextBlock Text="BYOAI v2" FontWeight="Bold"  VerticalAlignment="Center" />
                        <TextBlock Text="{Binding StatusText}" FontStyle="Italic" Opacity="0.7" Margin="10,0"  VerticalAlignment="Center" DockPanel.Dock="Right" />
                    </DockPanel>
                </Border>

                <!-- Messages List -->
                <ScrollViewer Grid.Row="1">
                    <ItemsControl ItemsSource="{Binding Messages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="BubbleBorder" CornerRadius="8" Padding="10" Margin="5,3" MaxWidth="500">
                                    <TextBlock Text="{Binding Content}" Margin="5" TextWrapping="Wrap" />
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding Role}" Value="user">
                                        <Setter TargetName="BubbleBorder" Property="Background" Value="#0E639C" />
                                        <Setter TargetName="BubbleBorder" Property="HorizontalAlignment" Value="Right" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Role}" Value="assistant">
                                        <Setter TargetName="BubbleBorder" Property="Background" Value="#2D2D30" />
                                        <Setter TargetName="BubbleBorder" Property="HorizontalAlignment" Value="Left" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Input Area -->
                <Border Grid.Row="2" Padding="10" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                                 AcceptsReturn="True" MaxHeight="100" Padding="5" 
                                   BorderThickness="0" />
                        <Button Grid.Column="1" Content="Send" Margin="5,0,0,0" Command="{Binding SendMessageCommand}" />
                    </Grid>
                </Border>
            </Grid>

            <!-- Memory View -->
            <Grid Visibility="{Binding IsMemoryActive, Converter={StaticResource BooleanToVisibilityConverter}}" DataContext="{Binding Memory}">
                <Border Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Text="Memory Snippets" FontSize="18" FontWeight="Bold"  Margin="0,0,0,10" />
                        
                        <Grid Grid.Row="1" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"   BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
                            <Button Grid.Column="1" Content="Search" Command="{Binding SearchCommand}" Margin="5,0,0,0" Padding="10,2" />
                        </Grid>

                        <ListBox Grid.Row="2" ItemsSource="{Binding Snippets}" Background="Transparent" BorderThickness="0" >
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="0,0,0,1" Padding="5">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" />
                                            <TextBlock Text="{Binding Content}" MaxHeight="40" TextTrimming="CharacterEllipsis" Opacity="0.6" FontSize="10" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>

            <!-- Settings View -->
            <Grid Visibility="{Binding IsSettingsActive, Converter={StaticResource BooleanToVisibilityConverter}}" DataContext="{Binding Settings}">
                <StackPanel Margin="20">
                    <TextBlock Text="Settings" FontSize="18" FontWeight="Bold"  Margin="0,0,0,20" />
                    
                    <TextBlock Text="OpenAI API Key" Foreground="Gray" Margin="0,10,0,5" />
                    <TextBox Text="{Binding OpenAiKey}"   BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
                    
                    <TextBlock Text="Ollama Endpoint" Foreground="Gray" Margin="0,10,0,5" />
                    <TextBox Text="{Binding OllamaEndpoint}"   BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
                    
                    <TextBlock Text="Gemini API Key" Foreground="Gray" Margin="0,10,0,5" />
                    <TextBox Text="{Binding GeminiKey}"   BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />

                    <CheckBox IsChecked="{Binding AutoSelectModels}" Content="Enable Auto Model Selection"  Margin="0,20,0,0" />
                    <TextBlock Text="The base model will decide which agent model to use per task." 
                               FontStyle="Italic" FontSize="10" Foreground="Gray" Margin="20,5,0,0" />

                    <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Margin="0,30,0,0" HorizontalAlignment="Left" Padding="15,5" />
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Overlay Busy Indicator -->
        <Border Grid.Column="1" Grid.Row="0" Background="#80000000" Visibility="{Binding Chat.IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="100" Height="10" />
                <TextBlock Text="Thinking..." Margin="0,10" Foreground="White" />
            </StackPanel>
        </Border>

        <!-- Footer Navigation -->
        <Border Grid.Column="1" Grid.Row="1" Padding="5" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
            <UniformGrid Columns="3">
                <Button Content="Chat" Command="{Binding NavigateToChatCommand}" />
                <Button Content="Memory" Command="{Binding NavigateToMemoryCommand}" />
                <Button Content="Settings" Command="{Binding NavigateToSettingsCommand}" />
            </UniformGrid>
        </Border>
    </Grid>
</DataTemplate>

